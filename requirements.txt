# install stays lean and you understand why every package is here.
requests>=2.31
pandas>=2.0

# geospatial: read planning-area polygons, clip the elevation grid to
# land, and do point-in-polygon joins. geopandas pulls in shapely + pyproj.
geopandas>=0.14

# Interactive maps: choropleth of per-area risk, saved as standalone HTML.
folium>=0.15

# Read the Copernicus GLO-30 elevation rasters (GeoTIFF) for the coastal layers.
rasterio>=1.3

# Trend stats (linear regression) + charts for the rainfall analysis.
scipy>=1.11
matplotlib>=3.7
