# Data — raw pulls and large/derived files are not committed.
# We keep the code that fetches them reproducible instead.
data/raw/
data/interim/
data/processed/
outputs/*.html
outputs/*.png

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Secrets / API keys
.env

# OS
.DS_Store
